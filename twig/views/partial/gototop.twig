{#<button onclick="topFunction()" id="gototop-btn" title="Go to top">Top</button>#}
<img onclick="topFunction()" id="gototop-btn" title="Go to top" src="{{ stylesheet_directory_uri }}/img/icona-a5tratto-w.png" alt="" class="img-fluid"  >
<script>
    //Get the button
    var gototop_btn = document.getElementById("gototop-btn");

    // When the user scrolls down 20px from the top of the document, show the button
    window.onscroll = function () {
        scrollFunction()
    };

    function scrollFunction() {
        if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
            gototop_btn.style.display = "block";
        } else {
            gototop_btn.style.display = "none";
        }
    }

    // When the user clicks on the button, scroll to the top of the document
    function topFunction() {
        document.body.scrollTop = 0;
        document.documentElement.scrollTop = 0;
    }
</script>